*.iml
.gradle
/local.properties
/.idea
.DS_Store
**/build/
/captures
.externalNativeBuild
.cxx
local.properties

# Python
__pycache__/
*.pyc

# Eval run artifacts (generated per-run by run.py — both platforms)
android-app/test/eval/runs/
ios-app-wip/test/eval/runs/

# Aggregate eval-results log appended after each run.py invocation
android-app/test/eval/results.log
ios-app-wip/test/eval/results.log

# Claude Code session-local state (loop progress, per-machine settings)
.claude/
